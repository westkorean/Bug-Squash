//We want to use 'x' as our output in our printf statement, but something is off with its value when we print it. Can you see the error?
#include <stdio.h>

int main(void){
    int x;
    printf("%d\n", x);

    return 0;
}
